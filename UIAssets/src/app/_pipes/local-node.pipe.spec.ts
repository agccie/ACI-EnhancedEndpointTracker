import { LocalNodePipe } from './local-node.pipe';

describe('LocalNodePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalNodePipe();
    expect(pipe).toBeTruthy();
  });
});
