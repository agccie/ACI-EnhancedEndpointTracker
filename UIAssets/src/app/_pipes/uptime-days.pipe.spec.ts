import { UptimeDaysPipe } from './uptime-days.pipe';

describe('UptimeDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new UptimeDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
